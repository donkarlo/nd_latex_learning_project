\documentclass{article}
\usepackage{tikz}
\usetikzlibrary{graphs, graphdrawing, shapes.geometric}
\usegdlibrary{force}

\begin{document}
    
    \begin{tikzpicture}
        \graph [
            spring layout,
            /graph drawing/iterations=300,
            /graph drawing/cooling factor=0.95,
            node distance=35mm,
            nodes={
                circle,
                draw,
                align=center,
                text width=3cm,
                inner sep=6mm
            }
        ] {
            A[as={This is a long text that will automatically break into multiple lines}];
            B[as={Another node with longer explanation inside}];
            C[as={Short text}];
            D[as={Yet another circle with some content}];
            
            A -> B;
            A -- C;
            B -- D;
            C -- D;
        };
    \end{tikzpicture}

\end{document}
